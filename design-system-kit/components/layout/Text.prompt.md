Use Text instead of raw headings so the scale stays intact. Never set a fifth font family.

```jsx
<Text variant="subtitle" as="h2">Strategy first, then execution</Text>
<Text variant="body-lg">Every engagement opens with an audit.</Text>
```
