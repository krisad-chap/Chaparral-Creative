Use Tabs to switch between sibling views of the same subject (channels in a report, sectors on a work page).

```jsx
<Tabs tabs={['Local government','Nonprofit','Service business']} onChange={setSector} />
```
